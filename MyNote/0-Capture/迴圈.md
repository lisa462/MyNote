- 計數式迴圈  
    迭代增加迴圈計數器時，用to  
    迭代減少迴圈計數器時，用downto  
    步進用by - 非1的數字進或退後

```
For 變數：=初值 To 終值
{
    指令序列;
}

```
```
For i：= 0 To 10
{
    i = i + 1;
}

```
迴圈次數：終值-初值+1

- 條件式迴圈

```
While (條件) do
{
    指令序列;
}
```
```
//當a不等於10，a + 1。
While (a != 10) do
{
    a = a + 1;
}
```